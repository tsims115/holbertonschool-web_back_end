#!/usr/bin/env python3
"""to_str takes a float and converts to a string representation"""


def to_str(n:float) -> str:
    """to_str takes a float and converts to a string representation"""
    return str(n)
